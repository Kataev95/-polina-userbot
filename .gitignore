__pycache__/
*.pyc
.env
*.session
*.session-journal
# Исходные файлы data/ (например fate_tasks.py) должны попадать в Git и deployment.
# Локальные данные при необходимости добавляйте отдельными правилами.
